// Familias tipográficas reales (cargadas vía @remotion/google-fonts en fonts.ts).
// Se reexporta desde aquí para mantener un único punto de import del theme.
export { fonts as typography } from './fonts';
